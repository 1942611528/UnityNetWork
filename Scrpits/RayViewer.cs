using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//射线绘制
public class RayViewer : MonoBehaviour
{
   // public LineRenderer line;
   // private PreFrameRaycast raycast;   //光线投射
    //public GameObject shootStart;//

   // private Vector3 screenCenterPoint;
    void Start()
    {
    
    }

    void Update()
    {
        //Vector3 ter= new Vector3(Screen.width / 2, Screen.height / 2, 0);
        //Vector3 fwd = transform.TransformDirection(Vector3.forward);//当前位置的方向
        //shootStart.transform.localPosition = fwd;
    }
}
